{
  "name": "sentient-wallet-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Local-only Sentient Wallet account-abstraction risk policy prototype",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.cjs --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "6.1.0",
    "hardhat": "2.28.0"
  }
}
